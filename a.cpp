#include <iostream>

int main() {

    std::cout << "started learning cpp again" << std::endl;
    std::cout <<"endl works like a <p> tag in html ";
    std::cout << "output will join together without endl"<< '\n';
    std::cout << "I need to make a flow chart tomorow "<< '\n';
    std::cout << "need to work on my goals"<< '\n';

    /* a \n also works like endl
     works for longer lines of cdde
     */ 
    // this can also work as  a comment but for a line of code

    return 0;
}